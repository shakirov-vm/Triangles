#pragma once

#include <algorithm>
#include <math.h>

#define eps 0.0001

bool equal_double(double one, double two);
bool great_double(double one, double two);
bool less_double(double one, double two);
bool GE_double(double one, double two);
bool LE_double(double one, double two);
